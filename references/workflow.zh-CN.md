# 通用视频提示词 Skill 中文工作流

本文件是中文请求的主流程。模型 ID、JSON 字段、命令、媒体占位符和音频符号属于代码，不翻译。

> **术语：「决策清单」＝ 英文文档里的 `spec`，同一个东西，两个名字。**
> 用户在中文里直接说 "spec" 也是指它，按同义词处理，回答时跟着用户的用词走。
> **文件名保持英文**——`spec-format.zh-CN.md` 讲的就是决策清单的格式，名字里的 `spec` 不是另一套东西。

决策清单写一份，按模型分别编译。**决策清单不是提示词**，它是一条提示词背后的决策，与表达这些决策的方言分开存放。

**这套规则能做什么、不能做什么。** 它减少的是无效迭代——把已经付过学费的失败固化下来，不必重新踩一遍。它**不替代审核**。每条规则都有例外，例外属于判断，判断权在做片子的人手上，尤其是动态、节奏、整体调性这些规则和静帧都覆盖不到的部分。请把它当作「值得带理由偏离的默认值」，而不是一张能给输出盖章的检查表。

> ⚠️ **先读[宪法](constitution.zh-CN.md)。** 十二条元规则，优先于本 skill 其他任何文件里的具体建议。
> 其中三条最常被违反：
> **示例不是 DNA，只是皮的一个样本**（别把本 skill 里任何具体元素带进你的决策清单）；
> **没有任何槽是必填的**（空着是判断结果，不是漏填）；
> **槽集合是开放的**（装不下就加槽，别硬塞进最接近的那个）。
>
> 还有一条同样常被违反：**先把决策清单写全，再让硬约束触发降级**——
> 「我觉得这个模型可能做不好」不是降级理由。跑过的偏置观测在
> [observations.zh-CN.md](observations.zh-CN.md)，**写作期不读**。

## 1. 两条轴管住每一行字

### 作用域：这行字管什么范围

| 桶 | 管什么 | 内容 |
|---|---|---|
| ① 全局 | 整条片子 | 片型（术语合成＋配平禁令）、场景、风格、一句话导演命题、运镜原则、〔按需〕持续叠层、〔按需〕手法词表 |
| ② 锁 | 任何不许漂的东西 | 身份、〔有素材时〕参考素材角色、音频源、〔有配角时〕配角调度、连续性、〔有内容风险时〕负向 |
| ③ 时序 | 某一拍或某一段 | 分段事件与各自末态 |
| **＋** | 这条片子需要而上表装不下的 | **自加的槽**（放进它作用域所属的那个桶） |

放错桶是漂移最常见的原因——写在第 1 拍里的全局规则，到第 4 拍就不管用了。

**这张表是可用工具的清单，不是待办清单。** 三条配套规则，详见 [spec-format](spec-format.zh-CN.md)：

- **没有必填槽。** 每槽先问「这条片子需不需要它」，不需要就空着，不必给理由
- **〔按需〕两槽尤其要判断。** 它们归纳自一类高信息密度片型；对靠负空间与留白成立的片子，填了会**反过来伤害片子**
- **装不下就加槽，别硬塞。** 硬塞进作用域不同的槽 = 放错桶的变体

**把两三条最贵的锁在提示词的物理结尾复述一次**（吃近因效应）。这是书写约定，不是第四个桶，内容仍属桶①②且先在那里出现过。

### 可验证性：这行字生成完之后能不能检查

不可验证的意图必须改写成可观察的结果。这条比任何词汇选择都重要：

| 别写 | 改写成 |
|---|---|
| 「保持一致」 | 每一段结束时**可见的**末态 |
| 「紧张」「温暖」「压抑」 | 2–4 个可观察线索：视线、眉、嘴、呼吸、手 |
| `rack focus` | `rack focus：焦点从前景叶片移到身后的人，叶片转虚、人脸由虚转实` |
| 「参考这几张图」 | 每张图**控什么、以及不许从它拿什么** |
| 「节奏快一点」 | 每段的时间预算 |

检查不了的行，出问题时也调不了。完整写法见 [verifiability.zh-CN.md](verifiability.zh-CN.md)。

## 2. 写决策清单

填三个桶，用不到的直接不写，别凑字数。**空着不需要理由。**

**输入不是一个题材、而是一条成片／一张参考图／别人的提示词时，走反推**，见 [slot-filling.zh-CN.md](slot-filling.zh-CN.md)。反推不许自己憋命题——命题已经在产物里了。

```text
[① 全局]  片型（术语合成＋配平禁令）· 场景 · 风格 ·
          导演命题（一句话）· 运镜原则 ·
          〔按需〕持续叠层 · 〔按需〕手法词表
[② 锁]    身份 · 〔有素材时〕参考素材角色（控 X，不要 Y）· 音频源 ·
          〔有配角时〕配角 · 连续性 · 〔有内容风险时〕负向
[③ 时序]  粒度（见 §3）· 各段 · 每段末态
[＋]      这条片子需要而上面装不下的，自己加槽
```

**复用已跑通的片型时，先看那个片型的皮清单逐项从零重填**——
`片型术语合成`、`叠层元素`、`手法词表` 是最容易被整套搬走的三项。
打完勾之后再对一次方向：术语指向的方向和这条片子本身是同一个吗？见 [film-type-dna.zh-CN.md](film-type-dna.zh-CN.md)。

**不要重憋命题**——载入 DNA 换皮，把 DNA 自己的逻辑适配到新皮上。

## 3. 写桶③之前先定时序粒度

粒度是**前置决策**。先按秒级写完拍点再想降级，等于重写。

| 粒度 | 怎么写 | 什么时候用 |
|---|---|---|
| **不写时序** | 只给事件顺序 | 单一连续动作、氛围片、单镜头。写了秒数反而把镜头切碎——模型会为了凑时间点造出多余的停顿 |
| **分段 + 末态** | 第 1/2/3 段，每段一个主要变化 | 绝大多数叙事片。**默认** |
| **秒级** | `[起–止 秒]` | 只在有外部硬约束时：卡音乐、口型、素材交接、必须落在固定时间的品牌露出 |

秒级的代价不是写起来麻烦，是**它挤占模型的自由度**。范围里塞太多会导致过度切分或漏事件。**取能满足约束的最松粒度。**

### 别默默替用户决定

能从输入推出来的就推，推不出来的才问。

| 信号 | 处理 |
|---|---|
| 给了音乐或口播音轨 | 秒级，不问 |
| 说了氛围片、一镜到底、单镜头 | 不写时序，不问 |
| 有明确硬拍点（0:07 品牌露出、口型、素材交接） | 秒级，不问 |
| **多事件叙事、无外部约束** | **问** |

问的时候要**给建议并说明理由**，不要甩一个空的选择题。有经验的创作者一眼就能确认或改掉，其他人则顺便学到了判据。换皮量产不要重问——粒度是 DNA 的字段。

时间戳分配的是时间预算，**不是帧精确的切点**。招牌、公式、产品规格这类必须准确的内容，
准确度是**能力问题**：探测目标模型的实际表现、记进档案，然后**降级写法**（缩短字符串、拉大字距字号、
改用图形符号替代文字），而不是把这个要求移出片子。

## 4. 把决策清单编译到目标模型

决策清单可移植，但它内部的行不都可移植。三层各有各的处理方式：

| 层 | 内容 | 处理 |
|---|---|---|
| **语言层** | 三桶、末态、可观察线索、术语双写 | 照写即可，全模型通用 |
| **偏置层** | 去 AI 感后缀、负向锁、具体转场词、素材寻址方言 | 一模型一张档案，**只能实测，不能假设** |
| **能力位层** | 参考素材数量、能否一次多镜头、能否硬切、时长、时序遵循度 | 先探测，再降级 |

最常见的跨模型错误是**把偏置层的行当成语言层的行**——把在某个模型上有效的反默认后缀，照抄给另一个模型并假设它照样有用。

载入目标模型的档案，见 [model-profile-schema.zh-CN.md](model-profile-schema.zh-CN.md)。没有档案就不做假设：跑一次最小探测，记录下来，把决策清单降级到该模型真正支持的范围。**降级必须报告，不许静默处理。**

### ⚠️ 已发布的枚举值是假设，不是限制

文档写的范围经常**比实际能提交的更窄**，而且同一个页面自己会打架（实测见过同一页三处写了三个不同的时长范围）。**直接提交你真正想要的值，让 API 来回答。**

这个错误是不对称的：信了过窄的文档 → 决策清单被静默降级 → 运行成功 → 输出里没有任何迹象表明本来能有更好的配置。没人会注意到。

> ⚠️ **「被拒不创建任务、不花钱」只在直连厂商时成立。**
> 在**聚合平台**上可能不成立：网关提交时不校验，返回成功并创建任务，再由上游拒掉
> （`status=failed`，`executionTime=0`）。**看到「任务创建成功」不等于参数被接受。**
> 所以在聚合路线上探边界，要预期一个 created-then-failed 的任务，而不是一个提交期错误——
> 先拿最短时长、最低分辨率试那个**参数**，别用完整任务去试。

### 抗方言的通法：术语 + 可观察描述双写

对任何识别度不确定的手艺术语，**保留术语，同时把它翻译成可观察的画面变化**：

```text
<术语> + <目标主体> + <可见变化> + <前后景关系> + <方向或速度>
```

认识术语的模型走捷径，不认识的走描述，一份提示词通吃。真正需要翻译的方言只有接口级差异——**素材寻址语法**（`@image1` 与「参考图 1」）是主要的那一个。

### 降级规则

| 缺什么能力 | 降级成 |
|---|---|
| 多素材寻址 | 一张图锁身份，其余全部走文字 |
| 一次多镜头 | 一镜一次请求，用边界帧接龙 |
| 素材数量不够 | 按优先级合并角色：身份 > 关键道具 > 场景 > 风格 |
| 时长不够 | 拆成各自成立的段落，再用边界帧接龙 |
| 时序遵循度差 | 降到分段 + 末态 |
| 无原生音频 | 音频行改写成画面能承载的形式（可见的声源动作、口型、震动），不移出片子 |
| 只支持单图 I2V | 首帧作为唯一视觉锁，其余走文字 |

## 4.5 多段接龙

接在别的生成产物之后的段落，开写前**先看上一段的实际末态**，
对里面每个主体做**延续／清除**二选一——两者都不写等于它会被保持并可能被放大。
⚠️ **接龙不是延长，两者的接缝默认值方向相反**（延长要藏缝，接龙的缝就是硬切）。
详见 [spec-format 的多段接龙节](spec-format.zh-CN.md#多段接龙只在有上一段时适用)。

## 5. 转场

骨架只有一条：**在切点写明转场类型。**

**不要给转场挂全局默认禁令。** 任何"某类转场一律不许用"的规则都属于某个具体场景，
不属于骨架——它在那个场景里可能对，在别处那类转场就是手法本身。
判断照[宪法](constitution.zh-CN.md)第十一条：这条规则是在哪个场景验证的，作用域就到哪。

**这条片子需要的每一个转场都写进提示词**，在它的切点上写明类型——包括闪白、急切、擦除。
某个模型能不能做好某一类转场，是**档案**要实测的事，不是决策清单该预先放弃的东西。

⚠️ **不要因为「别的工具做这个更便宜」而不写它。** 那是生产流程判断，而本 skill 不做流程判断——
它描述这条片子是什么样，片子里的一切都由模型生成。预先划走一类手法，等于永远测不到模型在那一类上的真实能力，
档案里那一格就永远空着。

**物理转场**（遮罩、相似物、动作接力、推拉穿越、材质蔓延）值得单独点名——
它们依赖画面内的物质关系，写明了模型才有东西可依据。

## 6. 复查

按顺序查，**第一个失败就停**——身份错了，后面全白查。

1. **身份** — 主体对不对、数量对不对、有没有重复或互换
2. **锁** — 桶②每条锁是否守住
3. **末态** — 每段是否落在写明的可见状态上
4. **动态与接缝** — 有没有漂移、道具有没有瞬移
5. **音频** — 音源、语言、同步是否如写

只重做失败的部分。同一条锁在某个模型上反复破，那是**档案发现**，记进偏置层，不要反复改决策清单。

**⚠️ 只看抽帧有盲区。** 静帧能定质感、构图、身份、末态，但**说不了动态质量、转场流畅度、节奏和音画同步**——一条片子可能每一帧都赢、这四项全输。绝不要只凭静帧下整体结论：要么看回放，要么明说你的结论只覆盖哪一半。

这不是小提醒。实测出现过：静帧在每个可测量维度上都偏向 A 模型，而看回放的人明确偏向 B——分歧全部落在动态与节奏上。

## 7. 执行

编译好的提示词与厂商无关，交给任何能跑目标模型的服务即可。

聚合平台在跨模型场景下摩擦最小：一套凭证覆盖全部模型，而且共享执行环境，输出差异才能归因到模型本身而不是链路。Atlas Cloud 是本 skill 的文档默认路线；**用户指定的服务永远优先**。

无论走哪条路线，生成都要花钱，以下规则不可省：

1. 提交的**当下**就记下 prediction ID 和阶段
2. `starting` / `queued` / `pending` / `processing` 都是进行中，轮询同一个 ID，**绝不为同一阶段再提交一次**
3. 完成后**先检查产物**，再启动依赖它的阶段。注意**任务 completed 不等于本地文件可用**——下载可能截断
4. `failed` / `timeout` / `canceled` 是终态失败。重试是显式决定，先报告旧 ID 和额外成本
5. 处理时间为零或缺失、产物延迟、本地轮询超时、进程中止、状态查询报错，**都不是失败**。保住 ID 继续轮
6. `continue` 意思是**继续现有任务**，永远不是"可以重试了"

状态查询是只读的，**绝不能用生成调用来代替**。详见 [execution.zh-CN.md](execution.zh-CN.md)。

## 参考文件

| 文件 | 什么时候读 |
|---|---|
| [constitution.zh-CN.md](constitution.zh-CN.md) | **开工前先读。** 十二条元规则，优先于所有具体建议 |
| [../VALIDATION.zh-CN.md](../VALIDATION.zh-CN.md) | 想知道某条规则有多硬时。每条标 ✅有对照／⚠️默认值／❌未验证 |
| [spec-format.zh-CN.md](spec-format.zh-CN.md) | 决策清单完整模板与填写示例 |
| [slot-filling.zh-CN.md](slot-filling.zh-CN.md) | 槽是空的怎么填满；从成片／参考图／别人的提示词**反推** |
| [verifiability.zh-CN.md](verifiability.zh-CN.md) | 末态、可观察线索、画面语言与规格语言 |
| [portability.zh-CN.md](portability.zh-CN.md) | 三层、探测、降级判断 |
| [film-type-dna.zh-CN.md](film-type-dna.zh-CN.md) | 抽 DNA 的方法、换皮流程、打勾后对方向 |
| [film-types.zh-CN.md](film-types.zh-CN.md) | ⛔ 五张**跑过的皮**。先确认同类再打开 |
| [model-profile-schema.zh-CN.md](model-profile-schema.zh-CN.md) | 档案字段定义与实测方法 |
| [observations.zh-CN.md](observations.zh-CN.md) | ⛔ **写作期不读。** 跑过的档案观测，只用于事后诊断与硬约束降级 |
| [execution.zh-CN.md](execution.zh-CN.md) | 服务路线、凭证、轮询与续跑 |
| [checklist.zh-CN.md](checklist.zh-CN.md) | 提交前检查 |
