# 验证状态与调试记录

这个 skill 还在调试期。本文件回答一个问题：**里面每条规则有多硬？**

> ⛔ **本文件不得用来决定"该不该写某样东西"。**
>
> 上一版这里写的是"写作期不读"——那条规则执行不了：**"想查某条规则有多硬"和"正在写"经常是同一时刻**，
> 而查证据强度本身是合法的。真正要防的不是"什么时候读"，是**读完拿它干什么**：
>
> | 合法 | 禁止 |
> |---|---|
> | 判断某条规则能不能拿去做决定（✅可以／⚠️当默认值／❌先做对照） | 因为某样东西"还没验证过"就不写它 |
> | 找下一步该补哪个对照 | 因为某条观测"是负面的"就删减决策清单 |
>
> 判据一句话：**它管的是"这条规则有多硬"，不管"这条片子该写什么"。**
> 后者只看这条片子要什么（[宪法](references/constitution.zh-CN.md)第五、十条）。

因为混着放会出事：一条有受控 A/B 的规则和一条我自己想出来的漂亮规则，
读起来分量一样，但一条能拿去做决定，另一条只能当默认值。

三级标记，全 skill 通用：

| 标记 | 含义 | 能怎么用 |
|---|---|---|
| ✅ | **有受控对照**：只改这一个变量，其余一字不动，跑过 | 可以拿去做决定 |
| ⚠️ | **带理由的默认值**：有论证或有观察，但变量没隔离 | 当默认值，值得带理由偏离 |
| ❌ | **未验证**：想法、推论、或每次都写所以没有对照 | 不要当结论。用之前先做对照 |

> 一条纪律：**把 ❌ 写成 ✅ 是这套方法里代价最高的错误。**
> 已经发生过一次——`film-type-dna` 初版把五条候选条件并列写成已验证结论，
> 补跑 removal test 之后只有一条站得住。

---

## ✅ 有受控对照的

| 规则 | 在哪 | 对照怎么做的 |
|---|---|---|
| **配平优于删词** | spec-format · 配平禁令 | 三点对照：保留术语→画风漂 / 删术语→画风回来但丢造型质量 / 保留+成组定向禁令→两者都要到 |
| **写画面内容的负向锁，未观测到生效；改正向描述后生效** | spec-format · 负向 | 「不要牌点字符」两版均未观测到该内容消失；改成正向描述牌面立刻改善。⚠️ 表述刻意保持观测式（宪法九） |
| **持续叠层是那个子片型的 DNA** | film-type-dna · 平面设计式 PV | v1 无此层 ↔ v2 起有，本片型里差距最明显的一处。⚠️ 适用范围仅该子片型 |
| **粒度：秒级与分段无可测差异** | film-type-dna · 粒度 | 同决策清单只改 6 个拍点标注，其余一字不动，每臂 n=2。切点/近静止帧/末态/事件顺序全部无差异 → 按「取最松粒度」定分段 |
| **可验证 ≠ 锁对了维度** | verifiability | 锁「平面性」两个模型都满足、都给光滑霓虹描边；换成「看得见工具痕迹」（蜡笔/排线/毛边）同一模型表现反转 |
| **指定机制，不只写效果** | verifiability | 「横跨天顶」（尺度）改成「大厅正中浮现」（体积）后更差——删掉尺度锚点后模型改走体积路线，撞碎了平面锁 |
| **prompt 长度耐受性是模型相关的** | portability | 同一份长 prompt：SD2 全保住并自己加了投影；H3 三样全丢（手/小骨架/质感） |
| **已发布的枚举值是假设不是限制** | portability | 三次证伪：H3 页面自相矛盾但支持 15s；Kling Turbo、Kling Pro 均超出参数表 |
| **具名版权词不拦**（Atlas + H3 路线） | model-profile · H3 | 2026-08-10：片名 + 角色名直接写，两臂（768P/2K）提交期与上游均通过，完整出片 |
| **`executionTime=0` 不代表失败** | model-profile · H3 | 同上两臂都是 0，都正常出片。该字段不能单独作为 failed 判据 |

## ⚠️ 带理由的默认值

| 规则 | 为什么还不是 ✅ |
|---|---|
| **多轴术语合成比单个标签有效** | 观察到的对照里，风格词与其他六七处改动是同时变的。因果没隔离 |
| **配平禁令要紧贴术语，不进负向槽** | 两份真值里作者自己不一致（一份在开头、一份在结尾负向段）。选「紧贴」的理由是可迁移性论证，不是对照 |
| **密度不来自切得更碎** | 对照的两条片子是不同作者、不同题材（12 切/26.77s vs 17 切/15s），不是受控变量 |
| **术语必须有真实语料 / 加参照物锚点** | 有判据，无对照 |
| **三桶划分本身** | 整套方法的骨架，不是可对照的单条规则 |
| **示例隔离四规则**（宪法一～四） | 2026-08-11 **第一次实证，但带两个限定，不够 ✅**。橘猫片：先只看参考片与题材把槽写完并落盘，再打开 case-library 与 film-types 逐项比对——片型术语与 B1/E1 **零重叠**；手法词表与 B3/E4 只重叠一项且用法不同；持续叠层的判断**没有**改回 E2 的"空着"。打开后只改了三处措辞，**全部由判据驱动**（末态可观察性 / 画面先行 / 粒度纯度），无一由内容驱动。<br>⚠️ **限定一**：片型不是全新的——落进「动画 / 风格化 2D」，四条 DNA 全中。所以测到的是"**同片型下不抄具体元素**"，**跨片型的引力未测**，而 8/10 出事恰恰是跨片型。<br>⚠️ **限定二**：这次有一条参考片当**外部锚点**，术语必须对得上它，本身就在排斥案例库。**从零填槽的隔离能力仍未测。** |
| **「动画 / 风格化 2D」那四条 DNA 是真 DNA** | 2026-08-11 旁证：在**没看过该条目**的情况下独立写出了全部四条（配平禁令成组＝DNA1；hold/wild take/禁平滑插值＝DNA2+4；纸面颗粒全片不掉＝DNA3），措辞完全不同、内容一致。⚠️ 但**同一个操作者、同一条片子**，n=1 |
| **禁令两问：正向优先 + 按严重程度筛**（spec-format） | 2026-08-11 加。来源是 Kiana 的经验积累，**不是本次观测**——本次没有做过"写禁令 vs 不写禁令"的受控对照。作用域从负向槽扩到所有槽里的禁，是**已有规则的作用域扩张**，风险低，但仍未验证 |
| **禁令第三问：作用范围会被放大到它所属的那一类**（spec-format） | 2026-08-12 加，两问变三问。<br>**实例一（✅ 有观测）**：MJ 参考图那轮，「不要三维／靠遮挡分前后／不许有影子」三条**完全生效**，同时把平面绘画自己的纵深手段（明度分层、平涂的形状影子、体块受光/背光两色块）一起挡掉，画面逐条合格但扁平单调；Kiana 给的第二条参考片证明这三样在纯平面里全部成立。<br>**实例二（❌ 推断，未观测）**：橘猫片早期版本写过 `没有音乐，没有旁白`，后续版本整段删除，成片里模型自带的音乐与音效跟着剧情走、Kiana 认为效果好。**但没有跑"保留该段"的对照**——「模型会把『没有音乐』读成整条音频轴静音」是 Kiana 基于经验的推断，不是本次测到的。<br>⚠️ 所以这条是**一次观测 + 一次跨维度推断**，不是两次观测。<br>要定它：同一份决策清单，只在某一条禁令的"点写法"与"点＋正面留白写法"之间切换，其余一字不动 |
| **力度形容词会被执行成"不发生事件"**（verifiability） | ⚠️ **归因不干净。** 失败观测是硬的（「全片最慢最安静的一段」→ 八秒空转、该段三件事一件没发生、近静止帧 75%）。但修法是在**同时改了三件事**的那一版里引入的（六段→九段 / 场景不固定 / 运镜改横向跟随），**"六段变九段"单独就可能修好它**。后两版复现，但都带着同一批捆绑改动。<br>要定它：同一份九段决策清单，只把谷底那段的写法在"力度词"与"动作＋因果"之间切换，其余一字不动 |
| **参考图按信息量选，不按叙事位置选**（slot-filling） | 2026-08-11 加。判据本身站得住（首帧的位置由叙事决定、参考图的只由信息量决定），但**只跑了"用专门造的设定图"这一条路，没跑"用第 0 帧当参考图"做对照** |
| **宪法第五、六条**（槽可选 / 槽开放） | 修的是一个已确认的失败，但修法本身还没验证。⚠️ 唯一一次实际用到"槽可选"是把持续叠层空着（案例库 E2），n=1 |
| **宪法第三条的三档处理 + 「规则本身也可以是皮」** | 2026-08-10 新写。触发它的失败是真的（宪法自己曾放着两条未验证的手法规则当反例），但**三档划分的边界只经过一次实践**，而且当天就复发了一次（在讲隔离的那一节里放了四个具体元素） |
| **宪法第十一条的改写 + 「方向相反就拆场景」** | 推论合理，但"接龙 vs 延长默认值相反"的依据是**一次观测**（四段接缝全是硬切且都是要的），不是对照 |
| **骨①：每个槽问自己的来源域** | ⚠️ **没有对照。** 依据是"三版都在同一体系里找工具、换体系后一次成"这个过程观察，但**没试过"从同一体系取"与"分开问"的受控对比**——中间还同时改了别的东西 |
| **骨②：接龙的延续／清除二选一** | ⚠️ 有一次前后对照（不写清除→首帧主体被放大 / 写清除→正确），但**那次同时改了两处**（还明写了人声），不能归因到单一变量 |

## ⚠️ 待核：一处记录与当前脚本复现不一致

**2026-08-12。** 橘猫片的 v4 工作文档里记着「v3 的动态量掉了一半（帧间差中位数 **1.041 → 0.439**）」。
用随 repo 发布的 `tools/analyze_rhythm.py` 重跑四个版本，得到的是：

| 版本 | YUV 突变 | 定格占比 | 帧间差中位数 |
|---|---|---|---|
| v1 | 5 | 88% | 0.130 |
| v2 | 3 | 64% | 0.161 |
| v3 | **21** | 54% | 0.139 |
| v4 | 6 | 60% | 0.104 |

**四版全在 0.10–0.16，与记录的 1.041 / 0.439 差一个数量级**，且看不出"掉了一半"。
可能是当时用了不同参数或不同版本的脚本，也可能测的不是这几个文件。
**在核清之前，「v3 动态量掉了一半」这条结论不成立，不得引用。**

顺带一个未解释的观测：**v3 的 YUV 突变 21 处，其余三版只有 3–6 处**。
而 v3→v4 按文档只改了神态词表、其余一字不动。需要看回放判断那 21 处是设计内的变化还是画面不稳定
（v4 提示词只允许两个特写切镜）—— 静帧与数值都判不了这件事。

## ❌ 未验证

| 项 | 状态 | 怎么验 |
|---|---|---|
| 平面设计式 PV 的 **②三色+语义分配** | v1–v5 每版都写，**没有一版不写** → 无对照。且父片型把配色列为皮 | 跑一次「不写该条、其余一字不动」 |
| 平面设计式 PV 的 **⑤平面感写成许可** | v4 才开始写，无 A/B | 同上 |
| **英文提示词** | 档案明写：中文实测有效，**英文未测**。具名风格术语的英文原形是否带同强度偏置未知 | 同一份决策清单中英各跑一遍 |
| **i2v 接龙的画风漂移** | 首帧带画风、提示词偏运动描述，四段接下来会不会逐段漂，完全未测 | 跑完四段看段3/段4 有没有出现肤色、渐变、立体感 |
| **H3 的时序抢跑** | ⚠️ **原记录被 2026-08-10 的数据部分推翻**，见下 | 见下 |

### 需要重测：H3 抢跑

原档案记：**抢跑且提前量累积**（4 阶段的 4/8/12s 落在 4/6/8s）。

2026-08-10 用 **6 个秒级区间**跑同类内容，**没观察到明显抢跑**——
六个事件基本落在写定的区间上（人群 0-2s / 广告牌 3-4s / 眼球 5-10s / 三格 11-13s / 全息 14s）。

两条记录不矛盾也不互相支持，因为**区间数量不同**。所以这个字段的正确状态是：
**与区间数量有关，具体关系未定**——不能当成一个常量去做补偿。

想定它：同一份内容，3 区间 / 6 区间各跑 n≥2，量每个事件的实际落点偏移。

---

## 变更记录

### 2026-08-11 · 第一次真的走完隔离流程（橘猫 × 光标，SD2.5 R2V 30s）

**这是 8/10 那六条骨写下来之后的第一次实证。** 流程严格照宪法第二条：
先只看参考片与题材把槽写完并落盘，**写完之后**才打开 case-library 与 film-types，逐项记录改了什么。
结论与两个限定见上表「示例隔离四规则」那一行——**限定必须跟结论一起引用，单引结论就是把 ⚠️ 谎报成 ✅。**

**落进骨的三条**（都进 ⚠️，无一进 ✅）：

| 改动 | 落点 |
|---|---|
| 禁令两问（正向优先 / 按严重程度筛），作用域扩到所有槽里的禁，并与宪法第十条划边界 | spec-format 新一节 |
| 力度与节奏也要落到可观察，「慢」会被读成"不发生事件" | verifiability 新一节 |
| 输入是一张图时先分清首帧还是参考图，两者选法不同 | slot-filling 路线表下 |

**筛掉没进骨的**（连同理由一起记，避免下次重提）：

- **配平禁令会禁过头 + 撤回要成组** —— 本次两次发作，但**两次不独立**：第二次是第一次没撤干净的直接后果，
  同一事件在延续，且同一操作者、同一天、同一条片子。判据部分（"这条禁令挡掉的东西里有没有我需要的"）
  已被上面第一条吸收；纪律部分（"记得成组检查"）是**纪律形状，纪律会失效**，不进
- **全局禁令守不住、落到段落守得住** —— 切镜那个实例站不住（v1/v2 写法几乎一样，切点 5→3 的差别
  可能来自别的变量；且 v3 运镜全解锁后切点反而涨到 10）。运镜那半成立但**无对照组**
- **参考素材"被同化"的第三种情况** —— 撤销。往前是素材生产（不归本 skill），
  往后是"不许从它拿什么"那一半的普通用法，**两头都不是新东西**
- **图与视频共用一份决策清单** —— 失败的本质是上下文管理，不是缺规则。
  "记得两边一起改"是纪律形状，必然失效。**不进**

> ⭐ **本次立的一条筛选尺子（Kiana）**：
> **凡是「记得做 X」形状的东西都不该进本 skill——纪律会失效**（宪法第三条自己说过）。
> **判据**（遇到这种情况按这个标准判）才可以进。用这把尺子重过一遍，候选从七条筛到三条。

**模型档案（能力位层，只进 SD2.5 那张表，绝不上升母版）**：

- 参考素材寻址：正文里 `@Image1` / `@Video1` / `@Audio1`，按提交顺序
- **`reference_images` 必须带 `data:image/jpeg;base64,` 前缀**；裸 PNG、裸 JPEG 均 `failed 1013036`
  （网关收下并创建任务，上游拒图，`inference: 0`）
- Atlas 网关前挂 Cloudflare：请求缺 `User-Agent` → 403 ＋ 响应体 `error code: 1010`（CF 的码，不是业务码）

> ⭐ **中间那条是宪法第八条第一次拿到反例**：
> **H3 是裸 base64 直通、加前缀反而不行；SD2.5 正好相反。**
> 在此之前"能力位层只能一模型一张档案、绝不上升母版"是个论证，现在有互相矛盾的实证了。

### 2026-08-10 · 示例污染的修复

**触发**：把「平面设计式 PV 片头」换皮到一部电影上，两项皮被整套继承——
叠层元素六项里四项原样搬；片型术语合成逐字搬走只删了两个字。

**为什么没被发现**：三色锁守住、配平禁令生效、末态逐条达成、叠层每帧都在——
**每条锁都在正常工作，工作的是另一部片子的方向。** 逐条检查在这种失败上是满分。

**根因不是疏忽，是文档结构**（三条同时成立）：

1. DNA 条目把示例元素与 DNA 写在同一条里，没标"这是一次填法"
2. **皮清单漏了「片型术语合成」和「叠层元素」两项**
3. 相邻三个槽（配平禁令 / 持续叠层 / 手法词表）的示例**全部同源**，形成默认答案的引力
4. `spec-format` 常见失败表里有一条「整片平淡 → 那两槽空着」，**直接把空着诊断成失败**，在推着人填

**改了什么**：

| 文件 | 改动 |
|---|---|
| `constitution.zh-CN.md` | **新建**。九条元规则，优先于所有具体建议 |
| `spec-format.zh-CN.md` | 槽位标〔按需〕+ 新增「槽集合是开放的」一节（加槽三问）+ 三处同源示例换成三个不同题材 + 改掉「空着=失败」那条诊断 + 新增两条失败模式（美术方向是别人的 / 给"空"的片子开"满"的处方） |
| `film-type-dna.zh-CN.md` | 第5条 DNA 的示例元素加警告框并标为皮 + 填槽先答「这套系统在做什么动作」+ 皮清单改表格补术语与叠层元素 + 换皮流程加「打完勾再对方向」+ 写入本次失败的完整记录 |
| `workflow.zh-CN.md` | 顶部加宪法引用 + 桶表标〔按需〕与开放槽 + 参考表首行加宪法 |
| `SKILL.md` | 加 §0 宪法节 + 桶表与模板块同步标注 |

**删除**：`spec-format.md`、`film-type-dna.md`、`model-profile-schema.md` 三个英文 references。
它们停在 8/4，描述的规则已被推翻（例如「持续叠层是固定槽」），**留着错文档比没文档更危险**。
交叉引用已全部改指中文版。git 主干有 8/4 版本，本地删除不丢内容。

**未做**：`verifiability.md`、`portability.md`、`execution.md`、`checklist.md` 四个英文版与中文同步（都停在 8/6），保留。
英文 references 的完整重写是一件单独的活。

### 2026-08-10（第二轮）· 示例物理隔离

第一轮只是把三处同源示例换成不同题材——那是**分散引力，不是消除引力**。
Kiana 指出应该把所有「皮」单独隔离，第二轮照这个做，范围是**2026-08-10 之前的全部内容**。

**新增宪法第三条：示例物理隔离，不靠自律。** 前两条是纪律，纪律会失效，所以结构上也要挡住。
宪法从九条变十条，条号顺移。

**新增 `references/case-library.zh-CN.md`（隔离区）**，按**片子**分组而不按槽分组——
要用其中一条，必须先承认"我的片子和这部是同一类"，那个动作本身是筛子。
四个案例：A 手绘 VFX（六个子条目）／B 平面设计式 PV（六个）／C 完整决策清单填写示例／D 零散小例（六个）。

**正文只留两样东西**（这个区分决定抽走之后正文还能不能用）：

| 留在正文 | 移进隔离区 |
|---|---|
| **失败的形状**——「锁被完全满足、同时完全失效」这类结构，不可抄只能理解 | **可被抄的元素**——术语字符串、禁令措辞、质感词、叠层清单、完整分镜 |
| **结构骨架**——占位符形式 `<你选的具名术语> ← 借:<...> 偏置:<...>` | 填好的实例 |
| **判据与动作顺序**——填这一槽的三步是什么 | 某一次填出来的结果 |

判断归属只问一句：**它能被直接复制粘贴进另一条片子的提示词吗？**

**逐文件**：

| 文件 | 处理 |
|---|---|
| `verifiability.zh-CN.md` | 234 → 217 行。四处高引力位置（承载概念的属性／机制／画面vs规格／噪音判据）全部改写为"失败的形状 + 指针"；画面vs规格对照表从具体措辞改成**措辞的形状**；末态、情绪、术语双写、参考素材四处示例移库 |
| `spec-format.zh-CN.md` | 配平禁令改为三步动作＋指针；叠层改为"先答这套系统在做什么动作"＋指针；手法词表、参考素材、导演命题、负向路由表、完整填写示例全部移库；顺带删掉一处内容重复 |
| `film-type-dna.zh-CN.md` | DNA 条目里的具体清单全部指针化（工具名、配平禁令措辞、术语字符串、水墨那次的长实测）；两处重复与一个多余的「关联」节删掉 |
| `checklist.zh-CN.md` | **新增「隔离」一节，排在最前**：五个检查项，含"术语是从这条片子重新选的吗""空着的槽是判断的结果吗" |

**残留核查**：正文里 蜡笔/粉笔/彩铅/赛璐璐/制图网格/量角器/准星/天顶/穹顶/恐龙/陶艺/墨不发光 全部为 0。
`portability` / `execution` 两个文件本来就零集中（讲机制不讲内容），未动。

### 2026-08-10（第三轮）· 两条定位纠正

**① skill 不做生产流程判断。** 原先五处写着「这个转场是不是该由剪辑来做」「必须准确的文字走后期」
——这类规则把一个**成本判断**（烧一次生成 vs 剪辑两秒钟）伪装成了**能力判断**，
后果是**预先划走一整类手法，那一格档案就永远填不上**。
Kiana 2026-08-10 定：**所有内容都基于模型的生成能力，skill 不考虑任何后期负责的事情。**
已全部改写（SKILL.md §5、workflow §3/§5、spec-format 手法词表、checklist、verifiability、portability、execution、model-profile）：
能不能做好某一类手法是**档案要实测的**，不是决策清单该预先放弃的；准确度不足时**降级写法**
（缩短字符串、拉大字距、改图形符号），而不是把要求移出片子。

**② 宪法新增第九条：不要把「你对模型能力的理解」写进规则。**（宪法 → 十一条）
档案里只记「我写了 X，观测到 Y」，不记「X 没用」。后者是跨槽的因果断言，而任何观测都是单槽的。
理由两条（Kiana 给的）：一个槽的作用会**溢出它自己的作用域**（写在音频槽里的东西改变了画面节奏）；
写不写一个槽看**是否服务于这条片子要的效果**，不看某个模型的能力。
同时把 H3 档案里那条「【音乐】段无效、可以删掉省预算」改写成中性观测。

⚠️ **同一天我自己又犯了一次**：从 n=1/臂 的两条样本得出「120 全面优于 132，调高数值是负收益」——
这是同一类断言。正确表述见下方「已观测」栏。

### 2026-08-10 · 节奏的受控对照（已观测，不是结论）

同一份决策清单只追加一个节拍骨架段、其余逐字不动，768P 各 n=1：

| 臂 | YUV 突变 | 定格占比 | 帧间差中位数 | 事件分布（每 3s） | 音轨点间隔 |
|---|---|---|---|---|---|
| 无音乐段 | 22 | 24% | 0.410 | [1,1,13,6,1] | 0.170s |
| 120 BPM | 27 | 22% | 0.478 | [1,3,13,8,2] | 0.265s |
| 132 BPM | 41 | 37% | 0.397 | [5,1,22,7,6] | 0.195s |

**可说的**：三条各不相同，两条带音乐段的音轨点间隔都比无音乐段大。
**不可说的**：「哪个 BPM 更好」「调高是负收益」——n=1/臂，且未测过**描述性写法**
（「密集鼓点」「双踩」「鼓点滚奏」而非数值），那可能是完全不同的一格。

另一条独立观测：**等间隔的计数动作会产出稳定节奏。** 写「共三次」「每道间隔零点一秒」之后，
出现了一段间隔精确 0.25s 的八连突变（4Hz）。与音乐段无关，属于桶③写法。

### 2026-08-10 · 新增的模型档案字段

`minimax/h3/image-to-video` 与 `reference-to-video` 的存在与 schema
（`image` 支持 Base64、`end_image` 可选尾帧、`ratio` 只有 `adaptive` 所以画幅由首帧继承）。
接龙路线的行为**尚未测**。

### 2026-08-07 · 检索叠层与秒级拍点

叠层从「测量语汇」换成「检索语汇」，每项给可数运动量（方框每半秒跳位、字符流每秒三分之一屏、波形每秒四到六次）。
实测**六项叠层运动全部落地**，且补回了前一版漏掉的一个事件。
新代价：**段内基调在 15 秒里换了五次**（亮→暗→亮→灰→暗），统一性下降。

### 2026-08-06/07 · H3 档案与粒度对照

见 `model-profile-schema.zh-CN.md` 的 H3 条目与 `film-type-dna.zh-CN.md` 的粒度节。

---

## 待验证队列（按信息量排序）

1. **示例隔离修复是否有效** —— 2026-08-11 橘猫片跑了一次，**有实证但带两个限定**（见上表）。
   还欠两格：**①跨片型的引力**（这次片型落进「动画 / 风格化 2D」，只测到同片型内）；
   **②从零填槽**（这次有参考片当外部锚点，锚点本身就在排斥案例库）。
   下一次选皮要**同时避开这两项**：一个跟五张皮都不同的片型，且**只给题材、不给参考片**
2. **力度形容词那条的干净归因** —— 同一份九段决策清单，只把谷底那段在「力度词」与「动作＋因果」
   之间切换，其余一字不动。**这次的修法跟"六段变九段"捆在一起，测不出因果**
3. **参考图 vs 首帧** —— 同一条片子，一版用专门造的设定图、一版用第 0 帧，其余一字不动
4. **骨① 的来源域分开问** —— 同一条片子，一版从画面体系顺手取动态词、一版分开问，其余一字不动
5. **骨② 的延续／清除** —— 单变量重跑一次（只改那一句，不动人声）
6. **②三色 / ⑤平面感许可的 removal test** —— 两条各跑一次，把猜测变成 DNA 或删掉
7. **H3 抢跑与区间数的关系** —— 3 / 6 区间各 n≥2
8. **中英文提示词的对照** —— 同一份决策清单编译成中文提示词与英文提示词各跑一遍，量 language sensitivity
   （模型档案里有这个字段，目前全部空着）。
   ⚠️ **别跟文档翻译混为一谈**：文档的中英双语已完成（10/10 同步，中文真源 → 英文译本）；
   这一条指的是**提示词语言本身对生成结果的影响**，未测

✅ **已验证出队**：i2v 接龙的画风漂移（2026-08-10，四段跑完零漂移，见 observations）

---

## 其他文档在哪

| 文档 | 位置 | 覆盖什么 | 缺什么 |
|---|---|---|---|
| 设计文档 | 维护者本地，**未随 repo 发布** | 最初的设计依据 | 后续全部改动 |
| 维护者笔记 | 维护者本地，**未随 repo 发布** | 决策、踩坑、进度、实测结论 | 不是 skill 的一部分 |
| **本文件** | skill 根目录 | 每条规则的证据强度 + 变更记录 + 待验证队列 | — |
